<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds talk to a local server over plain HTTP: BuildConfig.SERVER_URL defaults to
  http://10.0.2.2:8080, the emulator's view of the host. Since targetSdk 28 Android refuses
  cleartext traffic unless a config like this allows it, so only these local hosts are opened up.
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>
</network-security-config>
